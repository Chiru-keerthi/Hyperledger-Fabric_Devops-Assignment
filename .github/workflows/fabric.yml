name: Hyperledger Fabric CI/CD Pipeline (OPT-1)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # STEP 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # STEP 2: Deploy to EC2 via SSH
      - name: Deploy Fabric on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            echo "🚀 Starting Fabric Deployment on EC2"

            cd ~/fabric-project || exit

            git pull origin main

            cd fabric-samples/test-network

            echo "Stopping existing network..."
            ./network.sh down

            echo "Starting Fabric network with CA..."
            ./network.sh up createChannel -ca

            echo "Deploying chaincode..."
            ./network.sh deployCC \
              -ccn basic \
              -ccp ../asset-transfer-basic/chaincode-go \
              -ccl go

            echo "Verifying containers..."
            docker ps
